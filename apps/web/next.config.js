/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lp2.hm.com",
      "image.hm.com",
      "images.unsplash.com",
      "unsplash.com",
    ],
  },
};

export default nextConfig;
